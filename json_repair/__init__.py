"""
json_repair — Repair malformed JSON from LLM outputs.

Single-pass state machine that fixes the most common formatting errors
found in JSON generated by large language models:

- Unescaped double quotes inside strings
- Python-style triple-quoted strings (``\"\"\"``)
- CSV-style double-quote escaping (``\"\"``)
- Single-quoted strings
- Unquoted object keys
- Trailing commas
- Missing commas and colons
- Control characters in strings
- Extra text before / after JSON

Usage::

    >>> from json_repair import repair_json
    >>> repair_json('{"key": "value with "embedded" quotes"}')
    '{"key": "value with \\"embedded\\" quotes"}'
"""

from json_repair._repair import repair_json as repair_json

__all__ = ["repair_json"]
__version__ = "0.1.1"
